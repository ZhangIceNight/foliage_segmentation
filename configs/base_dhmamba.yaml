defaults:
  - base/base@_here_
  - _self_

model:
  model_type: "dhmamba"

optimizer:
  learning_rate: 0.001
  weight_decay: 0.00005
  max_epochs: 50
  eta_min: 0.000001
  warmup_epochs: 5


comet:
  name: "dhmamba-baseline"